$("#feed_entries_count").html("<%= '(' + @feed_entries.count.to_s + ')' %>")
$("#feed_entries").html("<%= escape_javascript(render @feed_entries) %>").fadeIn

// The code to show a loading animation while retrieving the feeds
jQuery(function($) {
  // create a convenient toggleLoading function
  //var toggleLoading = function() { $("#loading").toggle() };
  //var hideLoading = function() { $("#loading").hide() };
  //var showLoading = function() { $("#loading").show() };
  
  var fadeInLoading = function() { $("#loading").fadeIn() };
  var fadeOutLoading = function() { $("#loading").fadeOut() };  
  
  $("#feed_source_refresh")
  	.bind("ajax:beforeSend", fadeInLoading)
    .bind("ajax:complete", fadeOutLoading)
});